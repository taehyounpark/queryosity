```cpp

dataflow();

template <typename Key> 
dataflow(Key kwarg);

template <typename Key1, typename Key2> 
dataflow(Key1 kwarg, Key2 kwarg2);

template <typename Key1, typename Key2, typename Key3>
dataflow(Key1 kwarg, Key2 kwarg2, Key 3 kwarg);

```